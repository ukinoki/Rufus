language: cpp
compiler: gcc
sudo: require
dist: xenial

os:
  - linux

before_install:
  - sudo add-apt-repository ppa:beineri/opt-qt-5.10.0-xenial -y
  - sudo apt-get update -qq

install:
  - sudo apt-get -y install qt510base qt510serialport qt510websockets qt510multimedia
  - source /opt/qt*/bin/qt*-env.sh
  - sudo apt install libfontconfig1-dev pkg-config libjpeg-dev  gnome-common libglib2.0-dev gtk-doc-tools libyelp-dev yelp-tools gobject-introspection libsecret-1-dev libnautilus-extension-dev
  - wget https://poppler.freedesktop.org/poppler-0.50.0.tar.xz
  - tar -xvJf poppler-0.50.0.tar.xz
  - source /opt/qt510/bin/qt510-env.sh
  - pushd poppler-0.50.0 && cmake -DCMAKE_INSTALL_PREFIX=/usr && make && sudo make install && popd
  - sudo apt-get install poppler-utils poppler-data libpoppler-qt5-dev mysql-client

script:
  - qmake CONFIG+=release PREFIX=/usr
  - make -j$(nproc)
  - make INSTALL_ROOT=appdir -j$(nproc) install ; find appdir/
  - wget -c -nv "https://github.com/probonopd/linuxdeployqt/releases/download/continuous/linuxdeployqt-continuous-x86_64.AppImage"
  - chmod a+x linuxdeployqt-continuous-x86_64.AppImage
  - unset QTDIR; unset QT_PLUGIN_PATH ; unset LD_LIBRARY_PATH
  - export VERSION=$(git rev-parse --short HEAD)
  - ./linuxdeployqt-continuous-x86_64.AppImage appdir/usr/share/applications/rufus.desktop -bundle-non-qt-libs -appimage

after_success:
  - find appdir -executable -type f -exec ldd {} \; | grep " => /usr" | cut -d " " -f 2-3 | sort | uniq
  - # curl --upload-file Rufus.AppImage https://transfer.sh/Rufus-git.$(git rev-parse --short HEAD)-x86_64.AppImage
  - wget -c https://github.com/probonopd/uploadtool/raw/master/upload.sh
  - bash upload.sh Rufus.AppImage*

branches:
  only:
    - master
